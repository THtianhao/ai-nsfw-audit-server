version: "3.8"

services:
  nsfw:
    image: python:3.8-slim
    container_name: nsfw
    build: .
    ports:
      - "8000:8000"
    volumes:
        - ./:/app
    command:
      /bin/bash -c "pip install -r /app/requirements.txt && python /app/main.py"
